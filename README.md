# ArduinoScope
A simple browser-based graph of arduino pins using firmata

# How to run 
No package.json included, just do:

``` npm i microtime.js ```
``` npm i express ```
``` npm i socket.io ```

``` node scope.js ```